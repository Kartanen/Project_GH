Реализация метода max - def_max
Брифинг:
Если аргумент 1, считать его объектом, по которому можно пройтись циклом (iterable).
Если аргументов больше, объединить их в один список, который автоматически iterable.
